---
en:
  navbar:
    home: Home
    messages: Reports
    settings: Settings
  main_area:
    top:
      gpu_utilization_plot:
        title: GPU utilization on nodes
    add_node_form:
      title: Add node
      text_hint: Node IP address
      tooltip: Must be accessible via ssh without password
      invalid_format: Invalid format (required pattern %{pattern})
      add_button: Add
    node_list:
      title: Node list